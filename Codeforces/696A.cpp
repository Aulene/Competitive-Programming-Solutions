#include <bits/stdc++.h>

using namespace std;

#define int long long int
#define mod 1000000007
#define p push
#define pb push_back
#define mp make_pair
#define f first
#define s second

vector < vector < pair <int, int> > > vs;

signed main()
	{
		ios_base::sync_with_stdio(false);
		cin.tie(NULL);

		int i, j, u, v, w, x, y, z, q, op, n, m;

		cin >> m;

		while(m--)
			{
				cin >> q;

				if(q == 1)
					{
						cin >> u >> v >> w;

					}
				else
					{
						cin >> u >> v;
					}
			}
		return 0;
	}